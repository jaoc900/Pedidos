import 'package:flutter/material.dart';

class SizeData {
  final String size;
  final String percentage;
  final Color color;

  SizeData({
    required this.size,
    required this.percentage,
    required this.color,
  });
}